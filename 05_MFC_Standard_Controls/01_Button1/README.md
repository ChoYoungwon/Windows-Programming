### 코드 목적
코드를 이용한 다양한 버튼 만들기

### 주요 코드
- `CMy01Button1View::OnCreate()` : `CButton::Create()`함수를 이용해 서로 다른 스타일의 버튼 컨트롤을 생성함
- `CMy01Button1View::OnButtonClicked()` : 자체 제작 함수, View 클래스의 메시지 맵에 매크로를 추가함